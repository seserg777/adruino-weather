#ifndef ARDUINO_WEATHER_SECRETS_H
#define ARDUINO_WEATHER_SECRETS_H

// Copy this file to secrets.h and fill in your Wi-Fi credentials (secrets.h is gitignored if listed in .gitignore).

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

#endif
